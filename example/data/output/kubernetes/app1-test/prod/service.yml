kind: Service
apiVersion: v1
metadata:
  name: app1-test-dev-canary
  namespace: default
  labels:
    app: app1-test-dev-canary
spec:
  selector:
    app: app1-test-dev-canary
  ports:
      - name: https
        protocol: TCP
        port: 443
        targetPort: 10000
  type: LoadBalancer
